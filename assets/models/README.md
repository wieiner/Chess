# Runtime Model Assets

This tree contains reviewed runtime-ready assets and manifests. Applications may load only files reachable from a validated runtime manifest. Authored sources belong in `assets-source/models`; generated caches and temporary conversions belong in `.tmp`.
