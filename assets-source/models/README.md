# Authored Model Sources

This tree is the staging boundary for editable or high-detail model sources. Files here are not copied into application outputs automatically. Promote a reviewed, licensed, optimized derivative into `assets/models` through the import/validation pipeline.

Do not store credentials, private source paths, caches, temporary exports, or unreviewed archives here.
