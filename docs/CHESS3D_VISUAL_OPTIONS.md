# Chess3D Visual Options

P3C visual options are runtime-only WPF controls:

- Background theme: Neutral / Light / Dark.
- High contrast pieces.
- Show CoreCube overlay.
- Show Hodge arrows.
- Show Rubik layer overlay.

The toggles are intentionally not RuleProfile switches. A disabled overlay only hides UI decoration; it does not disable engine capabilities.
