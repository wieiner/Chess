# Matchmaking UI

ChessOnlineApp now exposes a small P3F/P4B matchmaking row:

- profile selector for the five existing Chess3D RuleProfiles;
- Join Queue;
- Cancel Queue;
- Queue Status.

When a match is found, the UI logs queue status and fills the matched room/table ids into the existing room/table fields so the operator can continue with ready/start controls.
